结论：本次应归类为 `supervisor 误判`，不是代码回归，也不是传感器问题。`wave94_gen2_model704_frontblock085_seed44` 的训练末尾标量虽然看起来像成功窗口，但对应 `quick eval` 实际是 `success_rate=0.0 / orbit_score=1.0 / progress_stall_rate≈0.667 / sensor_health_score=1.0`，已经被行为 gate 否决；问题在于 auto follow-up 仍按训练标量选中了它，并继续围绕 `frontblock` 做 0.77/0.80/0.82 的局部搜索。

我已只在允许路径内修复 [continuous_gen2_supervisor.py](/home/gwh/dashgo_rl_project/autopilot/continuous_gen2_supervisor.py#L516)。修复内容是：为 auto follow-up 增加 trial family 解析、从事件流恢复 gate 失败 run 和历史 auto-round family/已试值；后续自动选族会排除已被 `doctor/eval gate` 否决的 run，也会排除已经自动细化过但仍无正向结果的 family。没有改 [dashgo_env_v2.py](/home/gwh/dashgo_rl_project/dashgo_env_v2.py) 或 [train_v2.py](/home/gwh/dashgo_rl_project/train_v2.py)，因为当前证据不足以安全自动改训练合同；底层 orbit/stall 行为问题仍需新的 family 研究，而不是盲补 reward。记录已写入 [progress.md](/home/gwh/dashgo_rl_project/autopilot/progress.md) 和 [findings.md](/home/gwh/dashgo_rl_project/autopilot/findings.md)。

验证已完成：`python3 -m py_compile /home/gwh/dashgo_rl_project/autopilot/continuous_gen2_supervisor.py` 通过；静态调用 `build_auto_followup_round(4)` 后，候选不再回到 `frontblock`，而是转到 `progress` family，并显式排除了 `wave94` 以及已 autotune 过的 `frontblock/rearclear`。回归风险是这次修复只解决“错误继续搜哪一族”，不解决策略本身的 orbit/stall 退化。