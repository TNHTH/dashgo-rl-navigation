# Claude Code 环境完整迁移指南

> **版本**：v1.0
> **创建日期**：2026-01-16
> **适用场景**：在新电脑上复制已搭建好的Claude Code环境

---

## 目录

1. [环境架构概览](#环境架构概览)
2. [手动迁移方案](#手动迁移方案)
3. [自动化迁移方案](#自动化迁移方案)
4. [配置文件详解](#配置文件详解)
5. [常见问题排查](#常见问题排查)

---

## 环境架构概览

Claude Code 环境由以下几个层级组成：

```
┌─────────────────────────────────────────────────────────┐
│                    Claude Code 环境                      │
├─────────────────────────────────────────────────────────┤
│  1. 全局配置（~/.claude.json）                           │
│     - MCP服务器配置                                      │
│     - 全局插件设置                                       │
│     - API密钥                                            │
├─────────────────────────────────────────────────────────┤
│  2. 项目级配置（项目/.claude/）                          │
│     ├── instructions.md     # 项目指令                   │
│     ├── settings.json       # 项目设置                   │
│     ├── settings.local.json # 本地覆盖设置              │
│     ├── rules/              # 规则文件                   │
│     └── skills/             # 自定义技能                 │
├─────────────────────────────────────────────────────────┤
│  3. Git仓库（项目文件）                                  │
│     - 源代码                                             │
│     - 文档                                               │
│     - 配置文件                                           │
└─────────────────────────────────────────────────────────┘
```

---

## 手动迁移方案

### 方案A：完整克隆（推荐）

**适用场景**：新电脑可以直接访问当前GitHub仓库

#### 步骤1：克隆项目仓库

```bash
# 克隆GitHub仓库
git clone https://github.com/TNHTH/file-workspace.git
cd file-workspace
```

#### 步骤2：复制项目级配置

项目级配置已经包含在Git仓库中，无需额外操作。

**验证配置完整性**：

```bash
# Windows (PowerShell)
Get-ChildItem .claude -Recurse

# Linux/Mac
ls -la .claude/
```

应该看到以下结构：

```
.claude/
├── instructions.md
├── rules/
│   ├── dialogue-review-and-auto-update.md
│   └── file-organization.md
├── settings.json
├── settings.local.json
└── skills/
    ├── brainstorming/
    ├── dialogue-optimizer/
    ├── file-organizer/
    ├── kaizen/
    └── tapestry/
```

#### 步骤3：配置全局设置（可选）

如果需要在新电脑上使用相同的MCP服务器和全局配置：

**Windows**：
```powershell
# 复制全局配置到新电脑
# 从旧电脑：复制 C:\Users\YourName\.claude.json
# 到新电脑：粘贴到 C:\Users\YourName\.claude.json
```

**Linux/Mac**：
```bash
# 从旧电脑复制
scp ~/.claude.json user@new-computer:~/
```

⚠️ **注意**：全局配置包含API密钥等敏感信息，请谨慎处理。

---

### 方案B：选择性迁移

**适用场景**：只想迁移特定配置，不复制整个项目

#### 步骤1：创建导出包（在旧电脑）

```powershell
# Windows PowerShell
# 创建导出目录
New-Item -ItemType Directory -Path ".claude-export" -Force

# 复制核心配置
Copy-Item -Path ".claude\instructions.md" -Destination ".claude-export\"
Copy-Item -Path ".claude\settings.json" -Destination ".claude-export\"
Copy-Item -Path ".claude\rules" -Destination ".claude-export\" -Recurse
Copy-Item -Path ".claude\skills" -Destination ".claude-export\" -Recurse

# 压缩导出包
Compress-Archive -Path ".claude-export\*" -DestinationPath "claude-config.zip"
```

#### 步骤2：导入配置（在新电脑）

```powershell
# 解压导出包
Expand-Archive -Path "claude-config.zip" -DestinationPath ".\"

# 复制到新项目
New-Item -ItemType Directory -Path "new-project\.claude" -Force
Copy-Item -Path ".claude-export\*" -Destination "new-project\.claude\" -Recurse
```

---

## 自动化迁移方案

### 方案C：使用迁移脚本

我已为你创建自动化迁移脚本，支持Windows和Linux/Mac。

#### Windows版本：`migrate-claude-env.ps1`

```powershell
<#
.SYNOPSIS
    Claude Code 环境自动迁移脚本
.DESCRIPTION
    自动导出/导入 Claude Code 配置和技能
.EXAMPLE
    # 导出配置
    .\migrate-claude-env.ps1 -Action Export

    # 导入配置
    .\migrate-claude-env.ps1 -Action Import -SourcePath "claude-config.zip"
#>

param(
    [Parameter(Mandatory=$true)]
    [ValidateSet("Export", "Import")]
    [string]$Action,

    [Parameter(Mandatory=$false)]
    [string]$SourcePath = "claude-config.zip",

    [Parameter(Mandatory=$false)]
    [string]$OutputPath = ".claude-export",

    [Parameter(Mandatory=$false)]
    [string]$TargetProject = "."
)

function Export-ClaudeConfig {
    Write-Host "📦 开始导出 Claude Code 配置..." -ForegroundColor Cyan

    # 创建导出目录
    New-Item -ItemType Directory -Path $OutputPath -Force | Out-Null

    # 复制核心配置文件
    $filesToCopy = @(
        ".claude\instructions.md",
        ".claude\settings.json",
        ".claude\settings.local.json"
    )

    foreach ($file in $filesToCopy) {
        if (Test-Path $file) {
            Write-Host "  ✓ 复制: $file" -ForegroundColor Green
            Copy-Item -Path $file -Destination $OutputPath -Force
        } else {
            Write-Host "  ⚠ 跳过: $file (不存在)" -ForegroundColor Yellow
        }
    }

    # 复制规则目录
    if (Test-Path ".claude\rules") {
        Write-Host "  ✓ 复制: .claude\rules\" -ForegroundColor Green
        Copy-Item -Path ".claude\rules" -Destination "$OutputPath\" -Recurse -Force
    }

    # 复制技能目录
    if (Test-Path ".claude\skills") {
        Write-Host "  ✓ 复制: .claude\skills\" -ForegroundColor Green
        Copy-Item -Path ".claude\skills" -Destination "$OutputPath\" -Recurse -Force
    }

    # 创建打包信息
    $manifest = @{
        ExportDate = Get-Date -Format "yyyy-MM-dd HH:mm:ss"
        Version = "1.0"
        Files = Get-ChildItem -Path $OutputPath -Recurse | Select-Object -ExpandProperty FullName
    }

    $manifest | ConvertTo-Json | Out-File -FilePath "$OutputPath\manifest.json" -Encoding UTF8

    # 压缩导出包
    Write-Host "📦 压缩配置包..." -ForegroundColor Cyan
    Compress-Archive -Path "$OutputPath\*" -DestinationPath "claude-config.zip" -Force

    Write-Host "✅ 导出完成！文件保存至: claude-config.zip" -ForegroundColor Green
}

function Import-ClaudeConfig {
    Write-Host "📥 开始导入 Claude Code 配置..." -ForegroundColor Cyan

    # 检查源文件
    if (-not (Test-Path $SourcePath)) {
        Write-Host "❌ 错误: 找不到源文件 $SourcePath" -ForegroundColor Red
        return
    }

    # 解压到临时目录
    $tempDir = "$env:TEMP\claude-import-$((Get-Date).ToString('yyyyMMddHHmmss'))"
    Expand-Archive -Path $SourcePath -DestinationPath $tempDir -Force

    # 验证manifest
    if (Test-Path "$tempDir\manifest.json") {
        Write-Host "  ✓ 配置包验证通过" -ForegroundColor Green
    } else {
        Write-Host "  ⚠ 警告: 缺少manifest.json" -ForegroundColor Yellow
    }

    # 创建目标目录
    $targetClaudeDir = "$TargetProject\.claude"
    New-Item -ItemType Directory -Path $targetClaudeDir -Force | Out-Null

    # 复制配置文件
    if (Test-Path "$tempDir\instructions.md") {
        Write-Host "  ✓ 导入: instructions.md" -ForegroundColor Green
        Copy-Item -Path "$tempDir\instructions.md" -Destination $targetClaudeDir -Force
    }

    if (Test-Path "$tempDir\settings.json") {
        Write-Host "  ✓ 导入: settings.json" -ForegroundColor Green
        Copy-Item -Path "$tempDir\settings.json" -Destination $targetClaudeDir -Force
    }

    if (Test-Path "$tempDir\settings.local.json") {
        Write-Host "  ✓ 导入: settings.local.json" -ForegroundColor Green
        Copy-Item -Path "$tempDir\settings.local.json" -Destination $targetClaudeDir -Force
    }

    # 复制规则目录
    if (Test-Path "$tempDir\rules") {
        Write-Host "  ✓ 导入: rules\" -ForegroundColor Green
        Copy-Item -Path "$tempDir\rules" -Destination $targetClaudeDir -Recurse -Force
    }

    # 复制技能目录
    if (Test-Path "$tempDir\skills") {
        Write-Host "  ✓ 导入: skills\" -ForegroundColor Green
        Copy-Item -Path "$tempDir\skills" -Destination $targetClaudeDir -Recurse -Force
    }

    # 清理临时文件
    Remove-Item -Path $tempDir -Recurse -Force

    Write-Host "✅ 导入完成！配置已应用到: $targetClaudeDir" -ForegroundColor Green
}

# 主逻辑
switch ($Action) {
    "Export" {
        Export-ClaudeConfig
    }
    "Import" {
        Import-ClaudeConfig
    }
}
```

#### Linux/Mac版本：`migrate-claude-env.sh`

```bash
#!/bin/bash

# Claude Code 环境自动迁移脚本

set -e

ACTION=""
SOURCE_PATH="claude-config.zip"
OUTPUT_PATH=".claude-export"
TARGET_PROJECT="."

export_config() {
    echo "📦 开始导出 Claude Code 配置..."

    # 创建导出目录
    mkdir -p "$OUTPUT_PATH"

    # 复制核心配置文件
    for file in .claude/instructions.md .claude/settings.json .claude/settings.local.json; do
        if [ -f "$file" ]; then
            echo "  ✓ 复制: $file"
            cp "$file" "$OUTPUT_PATH/"
        else
            echo "  ⚠ 跳过: $file (不存在)"
        fi
    done

    # 复制规则目录
    if [ -d ".claude/rules" ]; then
        echo "  ✓ 复制: .claude/rules/"
        cp -r .claude/rules "$OUTPUT_PATH/"
    fi

    # 复制技能目录
    if [ -d ".claude/skills" ]; then
        echo "  ✓ 复制: .claude/skills/"
        cp -r .claude/skills "$OUTPUT_PATH/"
    fi

    # 创建manifest
    cat > "$OUTPUT_PATH/manifest.json" <<EOF
{
  "exportDate": "$(date -u +"%Y-%m-%d %H:%M:%S")",
  "version": "1.0"
}
EOF

    # 压缩导出包
    echo "📦 压缩配置包..."
    zip -r claude-config.zip "$OUTPUT_PATH/" > /dev/null

    echo "✅ 导出完成！文件保存至: claude-config.zip"
}

import_config() {
    echo "📥 开始导入 Claude Code 配置..."

    # 检查源文件
    if [ ! -f "$SOURCE_PATH" ]; then
        echo "❌ 错误: 找不到源文件 $SOURCE_PATH"
        exit 1
    fi

    # 解压到临时目录
    TEMP_DIR=$(mktemp -d)
    unzip -q "$SOURCE_PATH" -d "$TEMP_DIR"

    # 创建目标目录
    mkdir -p "$TARGET_PROJECT/.claude"

    # 复制配置文件
    find "$TEMP_DIR/$OUTPUT_PATH" -type f -exec cp {} "$TARGET_PROJECT/.claude/" \; 2>/dev/null || true

    # 清理临时文件
    rm -rf "$TEMP_DIR"

    echo "✅ 导入完成！配置已应用到: $TARGET_PROJECT/.claude"
}

# 参数解析
while [[ $# -gt 0 ]]; do
    case $1 in
        --export)
            ACTION="export"
            shift
            ;;
        --import)
            ACTION="import"
            shift
            ;;
        --source)
            SOURCE_PATH="$2"
            shift 2
            ;;
        --target)
            TARGET_PROJECT="$2"
            shift 2
            ;;
        *)
            echo "未知参数: $1"
            exit 1
            ;;
    esac
done

# 主逻辑
case "$ACTION" in
    export)
        export_config
        ;;
    import)
        import_config
        ;;
    *)
        echo "用法:"
        echo "  导出: $0 --export"
        echo "  导入: $0 --import --source claude-config.zip --target /path/to/project"
        exit 1
        ;;
esac
```

---

## 配置文件详解

### 1. instructions.md

**作用**：项目级AI指令，Claude会在每次对话开始时读取

**内容示例**：
```markdown
# 项目指令

- 使用中文回复
- 优先使用要点列表
- 遵循ULTRA-THINK协议
```

**迁移建议**：✅ 必须迁移

---

### 2. settings.json

**作用**：项目级设置，控制插件和MCP服务器

**内容示例**：
```json
{
  "enabledPlugins": {
    "feature-dev@claude-plugins-official": true
  }
}
```

**迁移建议**：✅ 建议迁移

---

### 3. settings.local.json

**作用**：本地覆盖设置，包含权限配置

**内容示例**：
```json
{
  "permissions": {
    "allow": ["Write", "Edit", "Read", "Bash"]
  },
  "autoConfirm": true
}
```

**迁移建议**：⚠️ 谨慎迁移（可能包含敏感权限）

---

### 4. rules/

**作用**：项目规则文件，AI必须遵守的规则

**文件列表**：
- `dialogue-review-and-auto-update.md` - 对话回顾规则
- `file-organization.md` - 文件组织规则

**迁移建议**：✅ 必须迁移

---

### 5. skills/

**作用**：自定义技能，扩展AI能力

**当前技能列表**：
- `brainstorming` - 头脑风暴
- `dialogue-optimizer` - 对话优化器
- `file-organizer` - 文件整理器
- `kaizen` - 持续改进
- `tapestry` - 内容提取和规划

**迁移建议**：✅ 必须迁移

---

### 6. ~/.claude.json（全局配置）

**作用**：全局Claude Code设置

**内容**：
- MCP服务器配置
- API密钥
- 全局插件设置

**迁移建议**：
- ⚠️ 不要直接复制（包含API密钥）
- ✅ 手动重新配置更安全

---

## 常见问题排查

### Q1: 导入后技能不显示

**原因**：skills目录结构不完整

**解决**：
```bash
# 检查技能目录结构
ls -la .claude/skills/

# 确保每个技能都有 SKILL.md 文件
find .claude/skills/ -name "SKILL.md"
```

---

### Q2: 规则文件不生效

**原因**：rules目录缺失或文件损坏

**解决**：
```bash
# 验证规则文件
cat .claude/rules/dialogue-review-and-auto-update.md

# 重新导入配置
.\migrate-claude-env.ps1 -Action Import -SourcePath "claude-config.zip"
```

---

### Q3: settings.local.json 权限错误

**原因**：Windows和Unix系统权限路径不同

**解决**：
```bash
# 删除settings.local.json，重新生成
rm .claude/settings.local.json

# 在Claude Code中重新设置权限
```

---

### Q4: MCP服务器连接失败

**原因**：全局配置中MCP服务器路径指向旧电脑

**解决**：
```bash
# 重新配置MCP服务器
# 在Claude Code设置中手动添加MCP服务器
```

---

### Q5: Git仓库冲突

**原因**：新电脑已有同名仓库

**解决**：
```bash
# 备份现有仓库
mv existing-repo existing-repo.backup

# 克隆新仓库
git clone https://github.com/TNHTH/file-workspace.git
```

---

## 快速开始检查清单

在新电脑上完成迁移后，使用以下清单验证：

- [ ] 克隆GitHub仓库成功
- [ ] `.claude/` 目录存在且完整
- [ ] `instructions.md` 文件存在
- [ ] `rules/` 目录包含规则文件
- [ ] `skills/` 目录包含5个技能
- [ ] Claude Code可以识别自定义技能
- [ ] 规则文件在对话中生效
- [ ] settings.json 配置正确

---

## 推荐迁移流程

**最佳实践**：

```
1. 在旧电脑上运行导出脚本
   ↓
2. 将 claude-config.zip 传输到新电脑
   ↓
3. 在新电脑上克隆GitHub仓库
   ↓
4. 运行导入脚本
   ↓
5. 验证配置完整性
   ↓
6. 测试Claude Code功能
```

---

## 安全建议

1. **不要提交敏感信息**：
   - API密钥
   - settings.local.json（可能包含敏感权限）
   - 个人笔记和分析报告

2. **使用.gitignore**：
   ```gitignore
   .claude/settings.local.json
   Si Yuan/分析报告/
   Si Yuan/对话记录/
   ```

3. **定期备份配置**：
   ```bash
   # 每月备份一次配置
   .\migrate-claude-env.ps1 -Action Export
   ```

---

**文档版本**：v1.0
**最后更新**：2026-01-16
**维护者**：Claude Code AI Assistant

---

## 相关文档

- [Claude-Skills完全使用指南](../Claude-Skills/Claude-Skills完全使用指南_2026-01-13.md)
- [File-Organizer自动维护说明](../File-Organizer自动维护说明.md)
- [对话回顾与自动更新机制](../../.claude/rules/dialogue-review-and-auto-update.md)
