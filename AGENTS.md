# AGENTS.md

## 作用范围与唯一来源
- 本文件适用于 `/home/gwh/dashgo_rl_project`，优先于上级目录中的 `AGENTS.md`。
- 本文件是本仓库唯一的长期代理规则来源。
- `agent.md` 与 `CLAUDE.md` 仅保留为兼容入口；如有冲突，一律以本文件为准。
- `.claude/rules/*.md` 与 `docs/05-协议规范/*.md` 仅作为技术背景或历史参考，不再作为主提示合同。
- 用户要求沉淀的长期偏好、长期工作流和项目级约束，统一写入本文件。

## 输出合同
- 默认使用中文回复，默认使用中文代码注释。
- 先给结论，再给执行细节。
- 对非琐碎请求，默认覆盖以下四项：
  1. 直接答案或明确判断。
  2. 步骤化依据、分析路径或解决过程。
  3. 备选方案、替代视角或关键取舍。
  4. 可立即执行的总结或行动方案。
- 问题较宽时，先拆成子问题再回答，不给空泛结论。
- 按任务采用合适的专业角色输出，例如工程师、调试者、评审者、教师或顾问。
- 充分利用可用上下文、工具和专业知识进行高强度推理；对外只输出可审计的关键依据与步骤，不承诺展示隐藏思维链或私有推理原文。

## 沟通、执行与安全默认
- 跳过无意义前奏，直接给可执行答案。
- 默认并行读取、搜索和核对多个文件。
- 批量编辑、结构调整、路径迁移或引用旧方案前，先扫描当前目录结构和历史路径是否仍然存在。
- 删除、覆盖、迁移、清理等不可逆操作前，先列出候选项，再执行。
- 引用仓库资料、历史记录或用户私有信息前，先搜索并确认文件或内容确实存在。
- 评审请求默认进入 review 模式：先列问题，再给简短结论。
- 调试请求先定位复现路径、错误源头和上下游影响，再决定修复动作。
- 长时间训练、重资源评测、部署、批量脚本运行、破坏性清理或仓库级重构前，先给简短执行计划并等待用户明确确认。
- 用户已经明确要求的局部代码或文档修改，可以直接实施，不必额外停下来确认。
- 当前仓库可能长期处于脏工作区；不要回滚、覆盖或打包提交与本次任务无关的改动。
- 新的长期规则统一写入 `AGENTS.md`，不要继续把动态规则堆到 `CLAUDE.md` 或手写 trigger 表里。

## 项目长期规则
- 项目定位：DashGo RL Navigation 是局部路径规划器，不是端到端导航器。策略负责短时局部避障与局部运动控制，全局路径规划不由 PPO 策略承担。
- 当前仓库结构以 `apps/isaac/`、`src/dashgo_rl/`、`configs/`、`drivers/`、`references/dashgo/`、`workspaces/` 为准；不要再假设历史上的 `dashgo/` 或 `multi-agent-system/` 路径仍然存在。
- 涉及 Isaac Lab、Isaac Sim、RSL-RL 的改动，优先核对目标版本的官方文档、当前仓库代码和现有适配层，避免直接套用其他版本 API 或旧示例。
- 修改训练入口或启动链路时，保持 Isaac Lab 启动不变量：`AppLauncher` 顺序、延迟导入、CLI 参数注册和仿真启动顺序不能被破坏。
- 修改训练配置适配链路时，保持 RSL-RL 配置扁平化与兼容逻辑，不要重新引入嵌套配置导致的初始化错误。
- 修改 `num_envs`、传感器、相机、日志量或其他资源敏感项时，要结合实际硬件预算说明取舍，不要盲目沿用大规模默认值。
- 涉及 Sim2Real 的物理参数、速度限制、轮距轮径或控制边界时，先回溯当前真实来源到 `drivers/`、`configs/robot/` 或 `references/dashgo/`，再修改仿真侧参数；必要时在代码或文档里说明来源。
- 对可能诱发“原地转圈”或其他局部最优的朝向类奖励、角度 shaping 和动作设计保持高警惕；若要恢复或加强，必须附带理由、风险说明和验证计划。
- 生成新的项目文档时，优先落到当前项目目录的合适位置，并为生成的 Markdown 文档写入明确的创建时间戳。

## 兼容与参考
- `CLAUDE.md` 只保留兼容入口说明，不再维护动态规则编号、频率统计、历史 Auto-Load Trigger 或失效路径。
- 旧规则文档中的技术经验可以参考，但只有在与当前目录结构、当前工具链和当前工作流一致时才可提升为本文件中的长期规则。
