# CLAUDE.md

> **兼容说明**: 2026-03-20 起，本仓库的长期代理规则已统一收敛到 `AGENTS.md`。

## 当前状态
- `AGENTS.md` 是唯一长期规则来源。
- 本文件仅为兼容旧脚本、旧笔记和旧入口名称而保留。
- 不再在本文件维护动态规则编号、频率统计、手写 Auto-Load Trigger、旧路径假设或临时工作流。

## 使用方式
- 需要查看或更新长期规则时，请直接读取或修改 `/home/gwh/dashgo_rl_project/AGENTS.md`。
- `agent.md` 也仅作为兼容别名存在，不维护独立规则正文。

## 参考资料边界
- `.claude/rules/*.md` 和 `docs/05-协议规范/*.md` 仍可作为技术背景参考。
- 只有被确认仍然有效、与当前仓库结构一致、且适合作为长期合同的内容，才应提升进 `AGENTS.md`。
