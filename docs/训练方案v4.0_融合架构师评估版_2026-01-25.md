# DashGo Navigation 训练方案 v4.0 - 融合架构师评估版

> **创建时间**: 2026-01-25 16:00:00
> **基于**: v3_robust_nav + 架构师评估报告
> **类型**: 融合改进方案（不执行，仅供评估）

---

## 🎯 方案设计理念

### 核心原则
1. **保持v3_robust_nav的稳定性**（学习率1.5e-4，熵系数0.005）
2. **采纳架构师的专业建议**（课程学习、明确PPO定位）
3. **平衡学习效率和收敛速度**（解决shaping过低的问题）
4. **长期可行性**（为Sim2Real部署做准备）

---

## 📋 Phase 1: 基础配置（0-1000迭代）

### 1.1 训练配置
```yaml
# train_cfg_v2.yaml
runner:
  experiment_name: "dashgo_v4_curriculum"
  run_name: "phase1_close_range"
  max_iterations: 1000  # 第一阶段1000轮

algorithm:
  learning_rate: 1.5e-4
  entropy_coef: 0.005
  clip_param: 0.2
  schedule: "adaptive"
```

### 1.2 奖励函数（第一阶段：近距离训练）
```python
@configclass
class RewardsCfg_Phase1:
    """
    课程学习阶段一：近距离导航（0-3米目标）

    目标：让机器人快速学会"移动有奖励"的基本概念
    策略：提高引导权重，降低收敛难度
    """

    # [主线任务] 到达终点
    reach_goal = RewardTermCfg(
        func=mdp.terminations.is_command_close,
        params={"command_name": "target_pose", "threshold": 0.5},
        weight=100.0  # 终点奖励（不是2500那么夸张，但足够明确）
    )

    # [增强引导] 靠近目标（权重翻倍，解决0.5过低的问题）
    shaping_distance = RewardTermCfg(
        func=mdp.rewards.position_command_error_tanh,  # 使用tanh映射
        weight=1.0,  # 从0.5提升到1.0（阶段一特供）
        params={"std": 2.0, "command_name": "target_pose"}
    )

    # [保持引导] 速度奖励
    target_speed = RewardTermCfg(
        func=reward_target_speed,
        weight=1.0,
        params={"asset_cfg": SceneEntityCfg("robot")}
    )

    # [保持引导] 对准奖励
    facing_goal = RewardTermCfg(
        func=reward_facing_target,
        weight=0.1,
        params={
            "command_name": "target_pose",
            "asset_cfg": SceneEntityCfg("robot")
        }
    )

    # [关键新增] 动作平滑约束（采纳架构师建议）
    action_smoothness = RewardTermCfg(
        func=mdp.rewards.action_smoothness,
        weight=-0.01,  # 从0.0001提升到-0.01（提升100倍，但不是-0.05那么激进）
        params={}
    )

    # [移除] velodyne_style_reward（简化奖励架构，避免冲突）
    # velodyne_style_reward = RewardTermCfg(...)  # 暂时移除

    # [安全约束] 碰撞惩罚
    collision = RewardTermCfg(
        func=penalty_collision_force,
        params={"threshold": 10.0},  # 从1.0放宽到10.0（避免误触发，但不是100）
        weight=-50.0
    )

    # [生存] 归零
    alive_penalty = RewardTermCfg(func=mdp.rewards.is_alive, weight=0.0)
```

### 1.3 命令生成器配置（限制目标范围）
```python
# 在 dashgo_env_v2.py 中修改命令生成器
commands_cfg:
    target_pose: commands.UniformPoseCommandCfg(
        asset_cfg=SceneEntityCfg("robot"),
        body_name="base_link",
        target_pos_range=(0.5, 3.0),  # ✅ 阶段一：限制在0.5-3.0米范围
        target_heading_range=(-3.14, 3.14),
        universally_sample=False,
        resample_time_range=(4.0, 8.0),  # 每4-8秒重新采样目标
    )
```

**阶段一预期效果**：
- ✅ 快速收敛（shaping_distance 1.0提供强引导）
- ✅ reach_goal > 20%（近距离目标容易达成）
- ✅ Policy Noise < 0.8（强平滑约束-0.01起作用）
- ✅ 机器人学会"移动+避障"的基本能力

---

## 📋 Phase 2: 进阶配置（1000-3000迭代）

### 2.1 训练配置
```yaml
# train_cfg_v2.yaml
runner:
  run_name: "phase2_long_range"
  max_iterations: 2000  # 第二阶段2000轮，总计3000轮

algorithm:
  learning_rate: 1.5e-4  # 保持不变
  entropy_coef: 0.005
  # ... 其他参数不变
```

### 2.2 奖励函数（第二阶段：远距离训练）
```python
@configclass
class RewardsCfg_Phase2:
    """
    课程学习阶段二：远距离导航（0-8米目标）

    目标：学会长距离导航和全局避障
    策略：降低引导权重，提高自主性
    """

    # [主线任务] 到达终点
    reach_goal = RewardTermCfg(
        func=mdp.terminations.is_command_close,
        params={"command_name": "target_pose", "threshold": 0.5},
        weight=100.0
    )

    # [降低引导] 靠近目标（权重减半）
    shaping_distance = RewardTermCfg(
        func=mdp.rewards.position_command_error_tanh,
        weight=0.5,  # 从1.0降到0.5（回到v3配置）
        params={"std": 2.0, "command_name": "target_pose"}
    )

    # [保持] 速度奖励
    target_speed = RewardTermCfg(
        func=reward_target_speed,
        weight=1.0,
        params={"asset_cfg": SceneEntityCfg("robot")}
    )

    # [保持] 对准奖励
    facing_goal = RewardTermCfg(
        func=reward_facing_target,
        weight=0.1,
        params={
            "command_name": "target_pose",
            "asset_cfg": SceneEntityCfg("robot")
        }
    )

    # [保持] 动作平滑
    action_smoothness = RewardTermCfg(
        func=mdp.rewards.action_smoothness,
        weight=-0.01,
        params={}
    )

    # [恢复] velodyne_style_reward（阶段二恢复综合导航奖励）
    velodyne_style_reward = RewardTermCfg(
        func=reward_navigation_sota,
        weight=1.0,
        params={
            "asset_cfg": SceneEntityCfg("robot"),
            "sensor_cfg": SceneEntityCfg("lidar_sensor"),
            "command_name": "target_pose"
        }
    )

    # [安全约束] 碰撞惩罚
    collision = RewardTermCfg(
        func=penalty_collision_force,
        params={"threshold": 10.0},
        weight=-50.0
    )

    alive_penalty = RewardTermCfg(func=mdp.rewards.is_alive, weight=0.0)
```

### 2.3 命令生成器配置（扩大目标范围）
```python
commands_cfg:
    target_pose: commands.UniformPoseCommandCfg(
        asset_cfg=SceneEntityCfg("robot"),
        body_name="base_link",
        target_pos_range=(0.5, 8.0),  # ✅ 阶段二：扩大到0.5-8.0米范围
        target_heading_range=(-3.14, 3.14),
        universally_sample=False,
        resample_time_range=(4.0, 8.0),
    )
```

**阶段二预期效果**：
- ✅ reach_goal > 30%（远距离目标，但引导权重降低）
- ✅ Policy Noise < 0.6（继续下降）
- ✅ 综合导航能力提升（velodyne_style恢复）

---

## 📋 Phase 3: 收敛优化（3000-5000迭代）

### 3.1 训练配置
```yaml
runner:
  run_name: "phase3_finetune"
  max_iterations: 2000
  # 从phase2的checkpoint继续训练

algorithm:
  learning_rate: 1.5e-4
  entropy_coef: 0.005
  # ... 保持不变
```

### 3.2 奖励函数（第三阶段：微调优化）
```python
@configclass
class RewardsCfg_Phase3:
    """
    课程学习阶段三：收敛优化

    目标：优化轨迹平滑度和任务完成率
    策略：微调权重，精细优化
    """

    # [主线] 到达终点
    reach_goal = RewardTermCfg(
        func=mdp.terminations.is_command_close,
        params={"command_name": "target_pose", "threshold": 0.5},
        weight=100.0
    )

    # [微调] 靠近目标
    shaping_distance = RewardTermCfg(
        func=mdp.rewards.position_command_error_tanh,
        weight=0.3,  # 进一步降低到0.3（更自主）
        params={"std": 2.0, "command_name": "target_pose"}
    )

    # [微调] 速度奖励（降低权重，避免速度过快导致碰撞）
    target_speed = RewardTermCfg(
        func=reward_target_speed,
        weight=0.5,  # 从1.0降到0.5
        params={"asset_cfg": SceneEntityCfg("robot")}
    )

    # [微调] 对准奖励
    facing_goal = RewardTermCfg(
        func=reward_facing_target,
        weight=0.05,  # 从0.1降到0.05（减少转圈）
        params={
            "command_name": "target_pose",
            "asset_cfg": SceneEntityCfg("robot")
        }
    )

    # [强化] 动作平滑（核心）
    action_smoothness = RewardTermCfg(
        func=mdp.rewards.action_smoothness,
        weight=-0.02,  # 从-0.01提升到-0.02（更强平滑）
        params={}
    )

    # [恢复] 综合导航
    velodyne_style_reward = RewardTermCfg(
        func=reward_navigation_sota,
        weight=1.0,
        params={
            "asset_cfg": SceneEntityCfg("robot"),
            "sensor_cfg": SceneEntityCfg("lidar_sensor"),
            "command_name": "target_pose"
        }
    )

    # [安全] 碰撞惩罚
    collision = RewardTermCfg(
        func=penalty_collision_force,
        params={"threshold": 10.0},
        weight=-50.0
    )

    alive_penalty = RewardTermCfg(func=mdp.rewards.is_alive, weight=0.0)
```

**阶段三预期效果**：
- ✅ reach_goal > 50%
- ✅ Policy Noise < 0.4
- ✅ 轨迹平滑（action_smoothness -0.02起作用）
- ✅ 任务完成率提升

---

## 🔄 与其他方案的对比

### vs. v3_robust_nav（当前方案）

| 维度 | v3_robust_nav | v4.0融合版 |
|------|---------------|-----------|
| **学习率** | 1.5e-4 | 1.5e-4（相同） |
| **熵系数** | 0.005 | 0.005（相同） |
| **训练轮数** | 4000 | 5000（分3阶段） |
| **shaping_distance** | 0.5（固定） | 1.0→0.5→0.3（动态） |
| **action_smoothness** | 0.0001（几乎为0） | -0.01（提升100倍） |
| **reach_goal奖励** | 无（包含在velodyne中） | 100.0（明确） |
| **velodyne_style** | 1.0 | 0→1.0→1.0（动态） |
| **target_speed** | 1.0 | 1.0→1.0→0.5（动态） |
| **collision阈值** | 1.0 | 10.0（放宽） |
| **目标范围** | 全范围 | 3m→8m→8m（课程学习） |

**关键改进**：
1. ✅ **采纳架构师建议**：action_smoothness权重提升100倍（-0.01）
2. ✅ **解决shaping过低问题**：阶段一提升到1.0，快速收敛
3. ✅ **引入课程学习**：目标范围从3m逐步扩大到8m
4. ✅ **明确终点奖励**：100.0（不是2500那么夸张）
5. ✅ **PhysX原生碰撞**：阈值10.0（不是100，也不是1.0）

### vs. 架构师v3.0 Ultimate方案

| 维度 | 架构师v3.0 | v4.0融合版 |
|------|-------------|-----------|
| **学习率** | 1.5e-4 | 1.5e-4（相同） |
| **shaping_distance** | 1.0（固定） | 1.0→0.5→0.3（动态优化） |
| **action_smoothness** | -0.05（激进） | -0.01（保守） |
| **reach_goal奖励** | 2500.0（极端） | 100.0（合理） |
| **velodyne_style** | ❌ 移除 | ✅ 恢复（综合导航） |
| **facing_goal** | ❌ 移除 | ✅ 保留（对准引导） |
| **target_speed** | ❌ 移除 | ✅ 保留（速度激励） |
| **collision阈值** | 100.0（过宽） | 10.0（平衡） |
| **课程学习** | ❌ 无 | ✅ 3阶段（3m→8m） |

**避免的架构师方案风险**：
1. ❌ **action_smoothness -0.05过强** → 降低到-0.01
2. ❌ **reach_goal 2500.0极端Sparse** → 降低到100.0
3. ❌ **移除5个奖励项** → 保留velodyne、facing_goal、target_speed
4. ❌ **collision 100.0过宽** → 降低到10.0

---

## 🎯 关键设计理念

### 1. 课程学习（采纳架构师建议）

**问题**：shaping_distance 0.5过低，可能导致收敛慢
**解决**：
- 阶段一（0-1000 iter）：权重1.0，目标范围0.5-3.0m
- 阶段二（1000-3000 iter）：权重0.5，目标范围0.5-8.0m
- 阶段三（3000-5000 iter）：权重0.3，进一步优化

**原理**：
> "先让机器人尝到甜头（近距离成功），再挑战长途奔袭（远距离导航）"

### 2. 明确PPO定位（采纳架构师建议）

**问题**：PPO不擅长长距离寻路
**解决**：
- 短期：目标范围限制（课程学习本身就是限制）
- 长期：PPO定义为Local Planner，配合ROS move_base全局规划

**原理**：
> "PPO只负责'走直线并避眼前障碍'，长距离寻路交给ROS move_base"

### 3. 动作平滑约束（采纳架构师建议）

**问题**：action_smoothness 0.0001几乎为0，导致Noise 17.30
**解决**：提升到-0.01（不是-0.05那么激进）

**原理**：
> "强平滑约束是解决Noise爆炸的关键，但-0.05可能让机器人不敢动，-0.01是平衡点"

### 4. 终点奖励明确（采纳架构师建议）

**问题**：终点信号不够明确
**解决**：新增100.0 reach_goal奖励（不是2500那么极端）

**原理**：
> "100.0足够大，让机器人知道'到达终点最重要'，但不会压倒一切导致Sparse奖励问题"

### 5. PhysX原生碰撞（采纳架构师建议）

**问题**：1.0阈值过于敏感
**解决**：提升到10.0（不是100.0那么极端）

**原理**：
> "对于圆盘形机器人，PhysX原生碰撞检测已经足够好且极快"

---

## 📊 预期训练曲线

### Phase 1 (0-1000 iter): 快速学习期
- Policy Noise: 1.0 → 0.8
- reach_goal: 0% → 25% （近距离目标容易达成）
- collision: 30% → 15%
- 特点：**快速收敛**（shaping 1.0强引导）

### Phase 2 (1000-3000 iter): 能力扩展期
- Policy Noise: 0.8 → 0.6
- reach_goal: 25% → 40% （远距离目标挑战）
- collision: 15% → 10%
- 特点：**技能泛化**（目标范围扩大）

### Phase 3 (3000-5000 iter): 收敛优化期
- Policy Noise: 0.6 → 0.4
- reach_goal: 40% → 55%+
- collision: 10% → 5%
- 特点：**精细优化**（轨迹平滑）

---

## 🚀 实施协议

### 阶段切换条件

**Phase 1 → Phase 2**（1000 iter后）：
```bash
# 检查点
if reach_goal > 20% and Policy Noise < 0.8:
    # 切换到阶段二
    修改dashgo_env_v2.py为Phase2配置
    修改命令生成器目标范围为8.0m
    从checkpoint恢复训练
```

**Phase 2 → Phase 3**（3000 iter后）：
```bash
# 检查点
if reach_goal > 35% and Policy Noise < 0.6:
    # 切换到阶段三
    修改dashgo_env_v2.py为Phase3配置
    从checkpoint恢复训练
```

### 失败回退机制

如果某阶段未达标（1000 iter后reach_goal < 15%）：
```bash
# 延长当前阶段
max_iterations += 500
# 继续训练，直到达标
```

---

## 💡 与架构师v3.0方案的决策点

### 我采纳的架构师建议

1. ✅ **课程学习**：3阶段，目标范围逐步扩大
2. ✅ **action_smoothness权重提升**：从0.0001到-0.01
3. ✅ **明确终点奖励**：新增100.0 reach_goal奖励
4. ✅ **PhysX碰撞阈值优化**：从1.0到10.0
5. ✅ **明确PPO定位**：Local Planner，不是End-to-End Navigator

### 我保留的当前方案优势

1. ✅ **Dense奖励架构**：保留velodyne_style、facing_goal、target_speed
2. ✅ **渐进式优化**：不是一次性大改，而是分阶段微调
3. **❌ 不采纳的架构师激进建议**：
   - action_smoothness -0.05（过强）→ 用-0.01
   - reach_goal 2500.0（极端）→ 用100.0
   - collision 100.0（过宽）→ 用10.0
   - 移除velodyne_style/facing_goal/target_speed → 保留

---

## 🎯 最终推荐

**推荐方案**：v4.0融合版（3阶段课程学习）

**理由**：
1. ✅ **融合两者优点**：v3的稳定性 + 架构师的专业洞察
2. ✅ **降低风险**：不是激进推翻，而是渐进优化
3. ✅ **提高成功率**：课程学习解决shaping过低问题
4. ✅ **长期可行性**：为Sim2Real部署做好准备

**不立即执行，先评估这个方案是否满足你的要求。**

---

**创建时间**: 2026-01-25 16:00:00
**维护者**: Claude Code AI System
**架构师评估**: ✅ 基于架构师深度评估报告
**状态**: 📋 评估中，等待用户确认
