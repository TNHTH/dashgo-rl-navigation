# Sim2Real完整部署指南 - 架构师修正版

> **创建时间**: 2026-01-28 00:00:00
> **版本**: v3.1-Architect
> **状态**: ✅ 代码已完成，待测试
> **基于**: 架构师建议 + 维度完全对齐

---

## 📋 部署前准备

### 硬件要求

| 组件 | 型号 | 说明 |
|------|------|------|
| **计算平台** | Jetson Nano 4GB / Ubuntu PC | 必须支持PyTorch |
| **机器人** | DashGo D1 | 差速驱动底盘 |
| **LiDAR** | EAI F4 | 360°，720点 |
| **操作系统** | Ubuntu 20.04 + ROS Noetic | 必须匹配 |

### 软件要求

```bash
# ROS Noetic
sudo apt install ros-noetic-desktop

# PyTorch (CPU版本)
pip install torch==1.10.0 torchvision==0.11.0

# 或Jetson上使用l4t-pytorch
# 参考: https://github.com/Qengineering/PyTorch-Jetson
```

---

## 🚀 部署步骤

### 第一步：导出模型（在训练机器上）

#### 1. 运行导出脚本

```bash
cd ~/dashgo_rl_project

# 启动Isaac Sim并导出模型
~/IsaacLab/isaaclab.sh -p export_torchscript.py
```

#### 2. 验证导出结果

**预期输出**：
```
================================================================================
[EXPORT] 正在分析模型输入需求...
================================================================================
  • 网络类型: GeoNavPolicy v3.1
  • 期望输入Shape: [1, 246]
  • 期望输入Dtype: torch.float32
  • 设备: cpu

  输入维度拆解:
    - LiDAR: 216 (72 × 3历史帧)
    - 其他状态: 30
    - 历史帧: 3

✅ 模型已导出至: catkin_ws/src/dashgo_rl/models/policy_torchscript.pt
   模型大小: 5.23 MB
```

#### 3. 检查文件

```bash
ls -lh catkin_ws/src/dashgo_rl/models/policy_torchscript.pt
# 应该显示约5MB
```

---

### 第二步：编译ROS工作空间

#### 1. 编译（在部署机器上）

```bash
cd ~/dashgo_rl_project/catkin_ws

# 编译
catkin_make

# 如果有依赖问题，先安装依赖
rosdep install --from-paths src --ignore-src -y
```

#### 2. 加载环境

```bash
source devel/setup.bash

# 添加到.bashrc（可选）
echo "source ~/dashgo_rl_project/catkin_ws/devel/setup.bash" >> ~/.bashrc
```

---

### 第三步：Gazebo仿真测试

#### 方案A：使用现有dashgo工作空间

```bash
# 1. 启动Gazebo仿真
cd ~/dashgo_rl_project/dashgo/1/1/nav
roslaunch launch/nav01.launch

# 2. 新终端：启动RL Agent
source ~/dashgo_rl_project/catkin_ws/devel/setup.bash
roslaunch dashgo_rl geo_nav.launch

# 3. 新终端：发送目标点
rostopic pub /move_base_simple/goal geometry_msgs/PoseStamped "header:
  frame_id: 'map'
pose:
  position:
    x: 2.0
    y: 1.0
  orientation:
    w: 1.0"
```

#### 方案B：使用RViz可视化

```bash
# 启动RViz
rosrun rviz rviz

# 添加以下显示项：
# 1. /scan (LaserScan)
# 2. /cmd_vel (Twist)
# 3. /odom (Odometry)
# 4. TF
```

---

### 第四步：实物部署

#### 1. 传输文件到Jetson

```bash
# 打包工作空间
cd ~/dashgo_rl_project
tar -czf catkin_ws.tar.gz catkin_ws/

# 传输到Jetson
scp catkin_ws.tar.gz jetson@dashgo:~/

# 在Jetson上解压
ssh jetson@dashgo
cd ~
tar -xzf catkin_ws.tar.gz
```

#### 2. Jetson上编译

```bash
# 安装依赖
sudo apt update
sudo apt install python3-pip python3-torch ros-noetic-tf2-geometry-msgs -y

# 编译
cd ~/catkin_ws
catkin_make
source devel/setup.bash
```

#### 3. 启动机器人

```bash
# Terminal 1: 启动底盘
roslaunch dashgo_bringup minimal.launch

# Terminal 2: 启动LiDAR
roslaunch dashgo_bringup lidar.launch

# Terminal 3: 启动RL Agent
source ~/catkin_ws/devel/setup.bash
roslaunch dashgo_rl geo_nav.launch
```

#### 4. 发送导航目标

```bash
# 使用RViz发送
rosrun rviz rviz

# 或命令行发送
rostopic pub /move_base_simple/goal geometry_msgs/PoseStamped "..."
```

---

## 🔍 验证清单

### ✅ 模型导出验证

- [ ] 文件存在：`catkin_ws/src/dashgo_rl/models/policy_torchscript.pt`
- [ ] 文件大小：约5MB
- [ ] 输入维度：[1, 246]
- [ ] 日志无错误

### ✅ ROS节点验证

```bash
# 检查节点是否加载
rosnode list | grep geo_nav_node

# 检查话题是否发布
rostopic list | grep -E "/scan|/cmd_vel|/odom"

# 检查话题频率
rostopic hz /cmd_vel
# 预期: 约20Hz
```

### ✅ 数据流验证

```python
# 添加到节点中的调试代码
def control_loop(self, event):
    # ...

    # 打印维度（仅在第一个episode）
    if not hasattr(self, '_debug_printed'):
        rospy.loginfo(f"观测维度: {input_tensor.shape}")
        rospy.loginfo(f"LiDAR: {lidar_data.shape}")
        rospy.loginfo(f"Goal: {self.goal_polar.shape}")
        rospy.loginfo(f"Velocity: {self.current_vel[:3].shape}")
        rospy.loginfo(f"Action: {action.shape}")
        self._debug_printed = True
```

---

## 🐛 常见问题排查

### 问题1：维度不匹配

**症状**：
```
RuntimeError: mat1 and mat2 shapes cannot be multiplied: [1, 77] vs [1, 246]
```

**原因**：
- 使用了助手版本的节点（77维）
- 模型需要246维

**解决**：
```bash
# 确认使用的是架构师修正版节点
head -20 catkin_ws/src/dashgo_rl/scripts/geo_nav_node.py | grep "架构师"
# 应该看到: "# 作者: Isaac Sim Architect"
```

### 问题2：模型加载失败

**症状**：
```
FileNotFoundError: [Errno 2] No such file or directory: 'policy_torchscript.pt'
```

**解决**：
```bash
# 检查模型路径
ls -l catkin_ws/src/dashgo_rl/models/

# 如果没有，运行导出脚本
~/IsaacLab/isaaclab.sh -p export_torchscript.py
```

### 问题3：TF查询失败

**症状**：
```
⚠️ TF查询失败
```

**原因**：
- frame_id不匹配
- TF树未建立

**解决**：
```bash
# 检查TF树
rosrun tf tf_echo map base_link

# 检查frame_id
rostopic echo /scan | grep frame_id
```

### 问题4：机器人不动

**症状**：
节点启动但机器人无反应

**排查步骤**：
```bash
# 1. 检查目标是否收到
rostopic echo /move_base_simple/goal -n 1

# 2. 检查/odom是否发布
rostopic hz /odom

# 3. 检查/cmd_vel是否发布
rostopic hz /cmd_vel

# 4. 启用调试日志
rosservice call /geo_nav_node/set_logger_level ros.DEBUG
```

---

## 📊 性能基准

### 预期性能指标

| 指标 | 仿真 (Gazebo) | 实物 (Jetson) |
|------|--------------|----------------|
| **控制频率** | 20 Hz | 15-20 Hz |
| **推理延迟** | <5 ms | <10 ms |
| **内存占用** | ~200 MB | ~150 MB |
| **CPU占用** | 20-30% | 40-60% |

### 优化建议

**Jetson优化**：
```bash
# 1. 使用Max-N模式（最大性能）
sudo nvpmodel -m 0

# 2. 启用GPU（如果安装了CUDA PyTorch）
# 在节点中修改：
self.device = torch.device("cuda")

# 3. 使用OpenVINO（进阶）
# 导出ONNX模型
# 使用OpenVINO工具链优化
```

---

## 📝 维护建议

### 日志记录

```bash
# 记录所有话题到bag文件
mkdir -p ~/bags/rl_test
rosbag record -O ~/bags/rl_test/test.bag \
  /scan \
  /odom \
  /cmd_vel \
  /move_base_simple/goal
```

### 参数调整

**常见调整**：
```xml
<!-- 在launch文件中调整 -->
<arg name="max_lin_vel" default="0.5" />  <!-- 提高最大速度 -->
<arg name="max_ang_vel" default="1.5" />  <!-- 提高最大角速度 -->
```

---

## 🎯 下一步

### 短期目标（本周）

1. ✅ 完成模型导出
2. ✅ Gazebo仿真测试
3. ⬜ 实物部署测试
4. ⬜ 性能优化

### 长期目标（本月）

1. ⬜ OpenVINO优化
2. ⬜ 安全过滤器集成
3. ⬜ 远程监控界面
4. ⬜ 自动化测试

---

## 📚 相关文档

- **架构师建议**: `docs/架构师建议.md`
- **评估报告**: `docs/架构师建议评估与分析报告_2026-01-27.md`
- **Geo-Distill V2.2**: `docs/Geo-Distill-V2.2-完整方案说明_2026-01-27.md`
- **Sim2Real方案**: `docs/DashGo-Sim2Real完整部署方案_v3.0_2026-01-27.md`

---

**维护者**: Claude Code AI Assistant (基于架构师方案)
**项目**: DashGo RL Navigation
**版本**: v3.1-Architect-Fixed
**日期**: 2026-01-28
**状态**: ✅ 代码完成，待测试验证
