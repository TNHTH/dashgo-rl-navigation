# 课程学习完整代码提取

> **创建时间**: 2026-01-26 00:05:00
> **目的**: 提供给架构师审查课程学习配置

---

## 1. 课程学习核心函数

**位置**: dashgo_env_v2.py 第321-365行

```python
def curriculum_expand_target_range(env, env_ids, command_name, start_step, end_step, min_limit, max_limit):
    """
    [v5.0 核心] 自动化课程学习
    根据当前训练总步数，线性扩展目标生成的距离范围 (3m -> 8m)
    """
    # 计算当前进度 alpha (0.0 ~ 1.0)
    current_step = env.common_step_counter

    if current_step < start_step:
        alpha = 0.0
    elif current_step > end_step:
        alpha = 1.0
    else:
        alpha = (current_step - start_step) / (end_step - start_step)

    # 计算当前难度
    current_limit = min_limit + (max_limit - min_limit) * alpha

    # 动态修改命令生成器的参数
    cmd_term = env.command_manager.get_term(command_name)
    if hasattr(cmd_term.cfg, "ranges") and hasattr(cmd_term.cfg.ranges, "pos_x"):
        cmd_term.cfg.ranges.pos_x = (-current_limit, current_limit)
        cmd_term.cfg.ranges.pos_y = (-current_limit, current_limit)
```

---

## 2. 课程学习配置

**位置**: dashgo_env_v2.py 第1075-1094行

```python
@configclass
class DashgoCurriculumCfg:
    """[v5.0 核心] 自动化课程学习配置"""

    target_expansion = CurriculumTermCfg(
        func=curriculum_expand_target_range,
        params={
            "command_name": "target_pose",
            "min_limit": 3.0,  # 初始难度：3米（新手区）
            "max_limit": 8.0,  # 最终难度：8米（专家区）
            "start_step": 0,
            "end_step": 300_000_000,  # ⚠️ 问题：基于4096环境计算
        }
    )
```

---

## 3. 训练配置

**文件**: train_cfg_v2.yaml

```yaml
runner:
  max_iterations: 5000        # 5000轮训练
  num_steps_per_env: 24       # 每轮每环境24步

algorithm:
  learning_rate: 1.5e-4        # 学习率
  entropy_coef: 0.005         # 熵系数

num_envs: 4096                # ⚠️ 问题：YAML配置是4096
```

---

## 4. 实际运行参数

**命令**:
```bash
~/IsaacLab/isaaclab.sh -p train_v2.py --headless --num_envs 32
```

**实际参数**:
- 环境数：32（命令行覆盖YAML的4096）
- 训练轮数：5000
- 每轮步数：24

---

## 5. 问题分析

### 当前配置的矛盾

**设计预期**（基于4096环境）:
```
总物理步数 = 4096环境 × 5000轮 × 24步 = 491,520,000步
end_step = 300,000,000步
完成进度 = 300M / 491.52M = 61%
```

**实际情况**（32环境）:
```
总物理步数 = 32环境 × 5000轮 × 24步 = 3,840,000步
end_step = 300,000,000步
完成进度 = 3.84M / 300M = 1.28%
```

**实际效果**:
- 目标范围：3m → 3.06m（几乎没扩展）
- ❌ 课程学习形同虚设！

---

## 6. 推荐修复方案

### 方案A：修复end_step（部分修复）

**修改**: dashgo_env_v2.py 第1048行

```python
"end_step": 3_840_000  # 从300M改为3.84M，适配32环境
```

**效果**:
- 5000轮训练完成100%课程学习
- 目标范围：3m → 8m完整扩展

**缺点**:
- 仍然只有5000轮训练（可能收敛不够）

---

### 方案B：增加训练轮数（完整修复）

**修改1**: train_cfg_v2.yaml

```yaml
runner:
  max_iterations: 50000  # 从5000增加到50000
```

**修改2**: dashgo_env_v2.py

```python
"end_step": 38_400_000  # 从300M改为38.4M，适配50000轮
```

**效果**:
- 50000轮训练完成100%课程学习
- 充分的训练时间
- 目标范围：3m → 8m完整扩展

**缺点**:
- 训练时间增加10倍（约60小时）

---

## 7. 计算验证

### 方案A验证

```python
# 32环境，5000轮，24步
total_steps = 32 × 5000 × 24 = 3,840,000

# end_step = 3,840,000
progress_at_5000 = 3.84M / 3.84M = 100% ✅
```

### 方案B验证

```python
# 32环境，50000轮，24步
total_steps = 32 × 50000 × 24 = 38,400,000

# end_step = 38,400,000
progress_at_50000 = 38.4M / 38.4M = 100% ✅
```

---

## 8. 架构师需要确认的问题

1. **end_step设计依据是什么？**
   - 为什么是300M？
   - 是基于4096环境计算的？

2. **是否需要完整的课程学习？**
   - 如果需要，建议方案B（50000轮）
   - 如果不需要，建议方案A（5000轮）

3. **当前训练是否需要重启？**
   - 如果采纳方案A：可以继续（只需修改end_step）
   - 如果采纳方案B：建议重启（需要50000轮）

---

**创建时间**: 2026-01-26 00:05:00
**维护者**: Claude Code AI System
**状态**: 等待架构师反馈
