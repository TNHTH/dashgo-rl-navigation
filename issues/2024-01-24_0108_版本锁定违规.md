# 问题记录：版本锁定违规

> **发现时间**: 2026-01-24
> **严重程度**: 🟡警告
> **状态**: ✅ 已解决
> **相关文件**: `train_v2.py`

---

## 问题描述

使用了不符合Isaac Sim 4.5标准的代码写法，手动定义了AppLauncher自动处理的参数。

**错误现象**:
- 代码中有大量兼容性注释（"兼容Isaac Sim旧版本"）
- 使用了手动参数定义，而非官方标准用法
- 不符合项目规则中的"版本锁定"要求

---

## 根本原因

### 1. 直接原因

**修改前的代码**（train_v2.py:19-22）:
```python
# [关键修复] 手动添加 AppLauncher 所需的标准参数
parser.add_argument("--headless", action="store_true", default=False, help="Run in headless mode.")
parser.add_argument("--enable_cameras", action="store_true", default=False, help="Enable camera sensors.")
parser.add_argument("--livestream", type=int, default=0, help="Enable livestreaming.")
```

**问题**:
- Isaac Sim 4.5的AppLauncher会**自动添加**这些标准参数
- 手动定义是多余的，且不符合官方标准
- 代码中还有兼容性注释，说明曾为旧版本做兼容

### 2. 深层原因

- 不了解AppLauncher的标准用法
- 没有查阅Isaac Lab官方文档
- 基于旧代码或经验编写，而非官方规范

---

## 解决方案

### 官方标准用法（已采用）

**修改后的代码**:
```python
def create_parser():
    """
    创建命令行参数解析器

    开发基准: Isaac Sim 4.5 + Ubuntu 20.04
    官方文档: https://isaac-sim.github.io/IsaacLab/main/reference/api/app_launcher.html

    Returns:
        argparse.ArgumentParser: 参数解析器

    说明:
        AppLauncher会自动添加标准参数（如--headless, --viewport等）
        这里只需定义用户自定义参数
    """
    parser = argparse.ArgumentParser(
        description="DashGo机器人导航训练脚本",
        formatter_class=argparse.ArgumentDefaultsHelpFormatter
    )

    # 用户自定义参数
    parser.add_argument("--video", action="store_true", default=False)
    parser.add_argument("--num_envs", type=int, default=16)
    parser.add_argument("--resume", action="store_true", default=False)
    parser.add_argument("--checkpoint", type=str, default=None)

    # AppLauncher会自动添加以下标准参数:
    # --headless: 启用headless模式（无GUI）
    # --viewport: 启用GUI渲染窗口
    # ... 更多参数见AppLauncher官方文档

    return parser
```

**关键改进**:
- ✅ 移除手动定义的标准参数
- ✅ 只定义用户自定义参数
- ✅ 添加完整的docstring和官方文档引用
- ✅ 移除兼容性注释

---

## 实施步骤

1. **重写train_v2.py**
   - 创建`create_parser()`函数
   - 移除手动参数定义
   - 添加文档注释

2. **添加官方文档引用**
   - 引用Isaac Lab官方文档
   - 说明参数来源和设计依据

3. **提交修复**
   ```bash
   git add train_v2.py
   git commit -m "refactor: 使用Isaac Sim 4.5标准AppLauncher用法"
   ```

4. **验证修复**
   ```bash
   python train_v2.py --help
   # 应该看到AppLauncher自动添加的标准参数
   ```

---

## 验证方法

### 代码审查
检查代码是否符合标准：
- ✅ 无手动定义`--headless`等标准参数
- ✅ 有完整的官方文档引用注释
- ✅ 无兼容性注释

### 功能测试
```bash
# 测试参数解析
python train_v2.py --help

# 应该看到AppLauncher自动添加的参数：
# --headless
# --viewport
# --num_envs
```

### 对比验证
| 项目 | 修改前 | 修改后 |
|------|--------|--------|
| 代码行数 | 146行 | 225行 |
| 标准参数 | 手动定义5个 | 自动添加10+ |
| 文档注释 | 无 | 完整docstring |
| 兼容性注释 | 有 | 已移除 |

---

## 经验教训

### 1. 官方文档优先
- ❌ 错误: 基于经验或旧代码编写
- ✅ 正确: 查阅官方文档，使用标准用法

### 2. 版本锁定意识
- ❌ 错误: 兼容多个版本，代码混乱
- ✅ 正确: 锁定Isaac Sim 4.5，移除旧版本兼容

### 3. 避免重复定义
- ❌ 错误: 手动定义框架已提供的功能
- ✅ 正确: 信任框架的默认行为

---

## 相关提交

- **commit**: `2de73a4` - refactor: 使用Isaac Sim 4.5标准AppLauncher用法
- **文件**: `train_v2.py` (78%重写)

---

## 相关文档

- **官方文档**: https://isaac-sim.github.io/IsaacLab/main/reference/api/app_launcher.html
- **参考示例**: `isaaclab/apps/isaac_lab.py`
- **项目规则**: `docs/05-协议规范/project-specific-rules.md` - 版本锁定要求

---

**问题状态**: ✅ 已解决
**解决时间**: 2026-01-24
**验证方式**: ✅ 代码审查通过，功能测试正常
