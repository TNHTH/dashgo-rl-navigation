# 架构师方案A本地化执行方案

> **创建时间**: 2026-01-29 18:00:00
> **严重程度**: 🔴 严重（诊断验证阶段）
> **状态**: 已制定方案，待用户确认后执行
> **相关文件**: sim2real_golden.launch, movebase.launch

---

## 📋 方案概述

**方案目标**：禁用RL节点，启用MoveBase控制，验证传统导航是否正常工作

**预期结果**：
- ✅ 如果传统导航正常 → **确诊RL模型问题** → 执行方案C（添加转动惩罚，重新训练）
- ❌ 如果传统导航仍然转圈 → **物理层问题**（摩擦力/动力学/万向轮参数）

---

## 🔍 本地环境分析

### 当前系统架构

**sim2real_golden.launch** 当前启动的组件：
1. ✅ Gazebo仿真（empty_world）
2. ✅ 机器人状态发布（robot_state_publisher）
3. ✅ SLAM建图（Gmapping）
4. ✅ Rviz可视化
5. ✅ **RL导航节点**（geo_nav_node.py）← **需要禁用**
6. ❌ **没有启动move_base** ← **需要添加**

### 发现的关键问题

**问题1：缺少move_base启动配置**
- `sim2real_golden.launch` 没有启动 `move_base` 节点
- 需要手动添加 move_base 的 include 语句

**问题2：cmd_vel topic冲突**
- RL节点（geo_nav_node.py）发布到 `/cmd_vel`
- move_base也会发布到 `/cmd_vel`
- 两个节点同时控制会导致冲突

**问题3：使用TEB而非DWA**
- `/dashgo/1/1/nav/param/base_local_planner_teb_params.yaml` 使用 TEB（Timed Elastic Band）
- 架构师建议使用 DWA（Dynamic Window Approach）
- TEB的参数配置与DWA不同

---

## 🛠️ 详细执行步骤

### 步骤1：备份当前launch文件（必须）

```bash
cd /home/gwh/dashgo_rl_project
git add catkin_ws/src/dashgo_rl/launch/sim2real_golden.launch
git commit -m "backup: 禁用RL节点前的基线（架构师方案A）

- 当前配置：RL节点（geo_nav_node.py）控制机器人
- 问题：机器人原地乱转，SLAM定位失败
- 下一步：禁用RL节点，启用move_base验证传统导航

Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>"
```

### 步骤2：禁用RL节点（geo_nav_node.py）

**文件**：`catkin_ws/src/dashgo_rl/launch/sim2real_golden.launch`

**操作**：注释掉第160-179行（RL导航节点）

```xml
<!-- ========================================================================
     RL导航节点（核心）- 🔴 [方案A] 暂时禁用，验证传统导航
======================================================================== -->
<!-- <node pkg="dashgo_rl" type="geo_nav_node.py" name="geo_nav_node"
      output="screen" required="true">

    # 模型路径
    <param name="model_path" value="$(arg model_path)" />

    # 速度限制（与dashgo_config.py一致）
    <param name="max_lin_vel" value="$(arg max_lin_vel)" />
    <param name="max_ang_vel" value="$(arg max_ang_vel)" />

    # 🔥 [新增] 加速度限制参数（安全关键）
    <param name="max_lin_acc" value="$(arg max_lin_acc)" />
    <param name="max_ang_acc" value="$(arg max_ang_acc)" />

    # 🔥 [修正] 控制频率（动态计算dt）
    <param name="control_rate" value="$(arg control_rate)" />
</node> -->
```

### 步骤3：添加move_base节点

**文件**：`catkin_ws/src/dashgo_rl/launch/sim2real_golden.launch`

**插入位置**：在第152行（`</group>`标签后，Rviz节点之前）

```xml
<!-- ========================================================================
     MoveBase导航（传统导航栈）- 🔴 [方案A] 启用验证
======================================================================== -->
<group if="$(arg enable_move_base)">
    <!-- move_base节点 -->
    <node pkg="move_base" type="move_base" respawn="false" name="move_base" output="screen" clear_params="true">

        <!-- Costmap配置 -->
        <rosparam file="$(find dashgo_rl)/param/costmap_common_params.yaml" command="load" ns="global_costmap" />
        <rosparam file="$(find dashgo_rl)/param/costmap_common_params.yaml" command="load" ns="local_costmap" />
        <rosparam file="$(find dashgo_rl)/param/local_costmap_params.yaml" command="load" />
        <rosparam file="$(find dashgo_rl)/param/global_costmap_params.yaml" command="load" />

        <!-- 局部规划器：使用DWA（架构师推荐） -->
        <rosparam file="$(find dashgo_rl)/param/base_local_planner_dwa_params.yaml" command="load" />

        <!--
        备选方案：使用TEB（当前dashgo默认）
        <rosparam file="$(find dashgo_rl)/param/base_local_planner_teb_params.yaml" command="load" />
        -->
    </node>

    <!-- 简单目标点订阅（用于测试） -->
    <node pkg="rostopic" type="rostopic" name="goal_listener"
          args="echo /move_base_simple/goal" output="screen"/>
</group>
```

### 步骤4：添加enable_move_base参数

**文件**：`catkin_ws/src/dashgo_rl/launch/sim2real_golden.launch`

**插入位置**：在第52行（`enable_rviz`参数后）

```xml
<arg name="enable_move_base" default="true"
     doc="是否启用MoveBase导航（方案A验证用）"/>
```

### 步骤5：复制move_base参数文件到项目目录

**原因**：
- dashgo/文件夹是只读参考（严禁修改）
- 需要复制参数文件到项目的 `catkin_ws/src/dashgo_rl/param/` 目录

**操作**：
```bash
# 创建参数目录
mkdir -p /home/gwh/dashgo_rl_project/catkin_ws/src/dashgo_rl/param

# 复制move_base相关参数文件
cp /home/gwh/dashgo_rl_project/dashgo/1/1/nav/param/costmap_common_params.yaml \
   /home/gwh/dashgo_rl_project/catkin_ws/src/dashgo_rl/param/

cp /home/gwh/dashgo_rl_project/dashgo/1/1/nav/param/local_costmap_params.yaml \
   /home/gwh/dashgo_rl_project/catkin_ws/src/dashgo_rl/param/

cp /home/gwh/dashgo_rl_project/dashgo/1/1/nav/param/global_costmap_params.yaml \
   /home/gwh/dashgo_rl_project/catkin_ws/src/dashgo_rl/param/

cp /home/gwh/dashgo_rl_project/dashgo/1/1/nav/param/base_local_planner_dwa_params.yaml \
   /home/gwh/dashgo_rl_project/catkin_ws/src/dashgo_rl/param/

# 检查文件是否复制成功
ls -lh /home/gwh/dashgo_rl_project/catkin_ws/src/dashgo_rl/param/
```

### 步骤6：测试验证（关键！）

**6.1 启动仿真**
```bash
cd /home/gwh/dashgo_rl_project
roslaunch dashgo_rl sim2real_golden.launch enable_move_base:=true
```

**6.2 在RViz中测试**
1. 等待地图建立（白色区域出现）
2. 点击 "2D Nav Goal"
3. 在机器人前方2米处点击
4. 观察：

**判断标准**：
- ✅ **如果机器人能正常导航**（沿规划路径前进，避开障碍物）
  - → **确诊RL模型问题**
  - → 执行方案C（添加转动惩罚）
  - → 重新训练模型

- ❌ **如果机器人仍然原地转圈**
  - → **物理层问题**（摩擦力/动力学/万向轮）
  - → 检查万向轮参数（mu1, mu2）
  - → 检查机器人转动惯量

### 步骤7：验证后清理

**如果验证成功（确诊RL问题）**：
```bash
# 提交方案A的修改
git add catkin_ws/src/dashgo_rl/launch/sim2real_golden.launch
git add catkin_ws/src/dashgo_rl/param/
git commit -m "feat: 方案A - 禁用RL节点，启用move_base验证

修改内容：
1. 注释掉geo_nav_node.py（RL节点）
2. 添加move_base节点（传统导航）
3. 复制move_base参数文件到项目目录
4. 验证结果：传统导航正常 → 确诊RL模型问题

下一步：执行方案C（添加转动惩罚，重新训练）

Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>"

# 推送到GitHub
git push
```

---

## 📊 关键差异：DWA vs TEB

### 架构师推荐：DWA（Dynamic Window Approach）

**优点**：
- ✅ 简单、高效、稳定
- ✅ 参数调节直观
- ✅ 对动态环境响应快
- ✅ ROS官方推荐

**配置文件**：`base_local_planner_dwa_params.yaml`

**关键参数**：
```yaml
DWAPlannerROS:
  max_vel_x: 0.3              # 最大线速度
  max_vel_theta: 1.0          # 最大角速度
  acc_lim_x: 1.5              # 线加速度限制
  acc_lim_theta: 1.0          # 角加速度限制
  sim_time: 1.7               # 前向模拟时间
  path_distance_bias: 32.0    # 路径距离权重
  goal_distance_bias: 24.0    # 目标距离权重
  occdist_scale: 0.01         # 障碍物距离权重
```

### 当前DashGo配置：TEB（Timed Elastic Band）

**优点**：
- ✅ 轨迹优化更精细
- ✅ 支持动态窗口调整
- ✅ 适合复杂环境

**缺点**：
- ⚠️ 参数调节复杂
- ⚠️ 对时间同步敏感
- ⚠️ 可能与Gmapping配合不佳

**配置文件**：`base_local_planner_teb_params.yaml`

**关键参数**（已从文件中读取）：
```yaml
TebLocalPlannerROS:
  max_vel_x: 0.3
  max_vel_theta: 1.0
  acc_lim_x: 1.5
  acc_lim_theta: 1.0
  xy_goal_tolerance: 0.2
  yaw_goal_tolerance: 0.2
  min_obstacle_dist: 0.1
```

### 建议策略

**方案A-1（保守）**：先使用DWA验证
- 理由：DWA更稳定，验证结果更可靠
- 如果DWA正常 → 确诊RL问题
- 如果DWA失败 → 再试TEB

**方案A-2（激进）**：直接使用TEB
- 理由：保持DashGo原有配置
- 但TEB可能与Gmapping配合不佳

**推荐**：方案A-1（使用DWA）

---

## ⚠️ 风险评估

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|---------|
| move_base参数缺失 | 中 | 中 | 从dashgo/复制到项目目录 |
| DWA参数不适配 | 低 | 中 | 先测试，再调整 |
| costmap配置错误 | 低 | 高 | 检查激光雷达topic名称 |
| TEB与Gmapping不兼容 | 中 | 高 | 优先使用DWA |

---

## 📝 验证检查清单

### Launch文件修改
- [ ] 备份当前sim2real_golden.launch
- [ ] 注释掉geo_nav_node.py节点
- [ ] 添加move_base节点
- [ ] 添加enable_move_base参数
- [ ] 复制move_base参数文件到项目目录

### 仿真测试
- [ ] 启动roslaunch dashgo_rl sim2real_golden.launch
- [ ] 等待Gmapping建图完成
- [ ] 在RViz中发送2D Nav Goal
- [ ] 观察机器人行为

### 判断标准
- [ ] 如果机器人能正常导航 → 确诊RL问题
- [ ] 如果机器人仍然转圈 → 物理层问题

---

## 🚀 下一步行动（根据验证结果）

### 情况A：传统导航正常（确诊RL问题）

**执行方案C**（添加转动惩罚）：

1. **修改 `dashgo_env_v2.py`**
```python
# 在奖励函数中添加
@configclass
class RewardTermCfg:
    shaping_angular_penalty: RewardTermCfg = RewardTermCfg(
        weight=0.5,  # 与参考项目一致
        params={}
    )

def compute_angular_penalty(self, actions: torch.Tensor) -> torch.Tensor:
    angular_vel = actions[:, 1]
    penalty = torch.abs(angular_vel)
    return penalty
```

2. **清理旧训练日志**（DR-018规则）
```bash
rm -rf logs/dashgo_v5_auto
```

3. **重新训练**
```bash
python scripts/train_v2.py --num_envs 80 --max_iterations 500
```

### 情况B：传统导航仍然转圈（物理层问题）

**检查物理参数**：

1. **万向轮摩擦力**
```python
# 检查 dashgo_d1_sim.urdf.xacro
<gazebo reference="caster_front_link">
  <mu1>0.0</mu1>  # 当前值
  <mu2>0.0</mu2>  # 当前值
</gazebo>

# 可能需要调整为：
# <mu1>0.1</mu1>
# <mu2>0.1</mu2>
```

2. **机器人转动惯量**
```python
# 检查惯性属性
<inertial>
  <mass value="5.0"/>
  <inertia ixx="0.1" ixy="0.0" ixz="0.0"
            iyy="0.1" iyz="0.0"
            izz="0.1"/>
</inertial>
```

---

## 📚 相关文档

### 架构师诊断
- `issues/2026-01-29_1745_SLAM定位失败导致原地乱转_架构师诊断.md`
- `issues/2026-01-29_1720_原地乱转问题_感知与RL双重诊断.md`

### 参考项目分析
- `.claude-temp/drl_nav_analysis/final_report.md` - 完整分析报告
- `.claude-temp/drl_nav_analysis/04_spinning_issue.md` - 原地乱转诊断

### DashGo配置
- `dashgo/1/1/nav/launch/movebase.launch` - 原始move_base launch
- `dashgo/1/1/nav/param/base_local_planner_dwa_params.yaml` - DWA参数
- `dashgo/1/1/nav/param/base_local_planner_teb_params.yaml` - TEB参数

---

**方案版本**: v1.0
**创建时间**: 2026-01-29 18:00:00
**状态**: 🟡 待用户确认后执行
**预计执行时间**: 30分钟（修改）+ 10分钟（验证）
**风险等级**: 低（仅用于验证，不破坏现有功能）
