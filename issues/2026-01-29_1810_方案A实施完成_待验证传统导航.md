# 方案A实施完成 - 待验证传统导航

> **创建时间**: 2026-01-29 18:10:00
> **严重程度**: 🔴 严重（诊断验证阶段）
> **状态**: 已实施代码修改，待用户验证
> **相关文件**: sim2real_golden.launch, move_base参数文件
> **提交记录**: commit dfd7a27, 51dc67c

---

## 📋 实施概述

**方案目标**：禁用RL节点，启用MoveBase控制，验证传统导航是否正常工作

**实施状态**：
- ✅ 代码修改完成（2个commit）
- ✅ Git提交完成
- ✅ 推送到GitHub完成
- ⏸️ **待用户验证测试**

---

## 🔧 已实施的修改

### 1. Launch文件修改（第一阶段 - commit dfd7a27）

**文件**：`catkin_ws/src/dashgo_rl/launch/sim2real_golden.launch`

**修改内容**：

#### 1.1 添加enable_move_base参数（第54行）
```xml
<arg name="enable_move_base" default="true"
     doc="是否启用MoveBase导航（方案A验证）"/>
```

#### 1.2 添加move_base节点（第175-188行）
```xml
<!-- ========================================================================
     MoveBase导航（传统导航栈）- 🔴 [方案A] 启用验证
======================================================================== -->
<group if="$(arg enable_move_base)">
    <!-- move_base节点 -->
    <node pkg="move_base" type="move_base" respawn="false" name="move_base" output="screen" clear_params="true">

        <!-- Costmap配置 -->
        <rosparam file="$(find dashgo_rl)/param/costmap_common_params.yaml" command="load" ns="global_costmap" />
        <rosparam file="$(find dashgo_rl)/param/costmap_common_params.yaml" command="load" ns="local_costmap" />
        <rosparam file="$(find dashgo_rl)/param/local_costmap_params.yaml" command="load" />
        <rosparam file="$(find dashgo_rl)/param/global_costmap_params.yaml" command="load" />

        <!-- 局部规划器：使用DWA（架构师推荐，稳定可靠） -->
        <rosparam file="$(find dashgo_rl)/param/base_local_planner_dwa_params.yaml" command="load" />
    </node>
</group>
```

#### 1.3 禁用RL节点（第193-209行，注释）
```xml
<!-- ========================================================================
     RL导航节点（核心）- 🔴 [方案A] 暂时禁用，验证传统导航
======================================================================== -->
<!-- <node pkg="dashgo_rl" type="geo_nav_node.py" name="geo_nav_node"
      output="screen" required="true">
    ...
</node> -->
```

### 2. 参数文件复制（第一阶段）

**目标目录**：`catkin_ws/src/dashgo_rl/param/`

**复制的文件**：
- ✅ `costmap_common_params.yaml` (946 bytes)
- ✅ `local_costmap_params.yaml` (872 bytes)
- ✅ `global_costmap_params.yaml` (845 bytes)
- ✅ `base_local_planner_dwa_params.yaml` (1.6 KB)

### 3. 用户测试发现的问题（第二阶段）

**测试结果**（2026-01-29）：
- ❌ `/cmd_vel` topic没有消息
- ❌ `rqt_graph`只显示5个节点（缺少move_base）
- ❌ Gmapping没有订阅激光雷达数据

**诊断分析**：
1. **move_base进程存在但未激活**：PID 41002存在，但rqt_graph未显示
   - 原因：`if="$(arg enable_move_base)"`条件判断问题

2. **Gmapping失明**：未配置scan_topic参数
   - 原因：缺少`<param name="scan_topic" value="/scan"/>`
   - 原因：缺少`<remap from="scan" to="scan"/>`

### 4. Launch文件修复（第二阶段 - commit 51dc67c）

**文件**：`catkin_ws/src/dashgo_rl/launch/sim2real_golden.launch`

#### 4.1 修复Gmapping激光雷达连接（第119-126行）

**添加的参数**：
```xml
<!-- 🔥 [修复] 添加激光雷达topic参数 -->
<param name="scan_topic" value="/scan"/>

<!-- 🔥 [修复] 添加显式话题remap -->
<remap from="scan" to="scan"/>

<!-- 🔥 [调试] 启用调试输出 -->
<param name="debug" value="true"/>
```

#### 4.2 修复move_base强制启动（第175行）

**修改前**：
```xml
<group if="$(arg enable_move_base)">
```

**修改后**：
```xml
<!-- 🔴 [修复] 移除if条件，确保move_base总是启动 -->
<group>
```

---

## 🧪 待验证项目

### 验证步骤（需要用户执行）

#### 步骤1：启动仿真
```bash
cd /home/gwh/dashgo_rl_project
roslaunch dashgo_rl sim2real_golden.launch
```

**预期结果**：
- ✅ Gazebo启动
- ✅ RViz启动
- ✅ move_base节点启动（应在rqt_graph中显示）
- ✅ SLAM（Gmapping）启动并连接激光雷达
- ❌ geo_nav_node.py不启动（已注释）

#### 步骤2：检查TF树
**新终端**：
```bash
rosrun tf tf_echo map base_link
```

**预期结果**：
- ✅ TF树正常连接
- ✅ 变换频率稳定（~10-50ms）
- ❌ 无TF_REPEATED_DATA警告（或警告数量减少）

#### 步骤3：检查Gmapping状态
**新终端**：
```bash
rostopic echo /slam_gmapping/entropy -n 5
```

**预期结果**：
- ✅ Gmapping输出熵值（证明已订阅激光雷达）
- ✅ 不再出现大量 "Scan Matching Failed"

#### 步骤4：检查move_base状态
**新终端**：
```bash
rostopic echo /move_base/status -n 5
```

**预期结果**：
- ✅ 状态为 "active" 或 "planning"
- ❌ 不报错或报 "No global plan"

#### 步骤5：发送2D Nav Goal
**在RViz中**：
1. 点击 "2D Nav Goal" 按钮
2. 在机器人前方2米处点击
3. 观察机器人行为

### 🔍 判断标准（关键！）

#### ✅ **情况A：传统导航正常**（期望结果）

**观察到的现象**：
- 机器人开始前进（线速度 > 0.1 m/s）
- 角速度平稳（不持续旋转）
- RViz中显示全局路径（绿线）
- RViz中显示局部路径（红色箭头）
- 机器人能避开障碍物（如果有）
- 最终到达目标点附近

**Gmapping日志**：
- ✅ 不再出现大量 "Scan Matching Failed"
- ✅ Likelihood值 > 0（或接近0）
- ✅ Laser Pose坐标平稳变化（不剧烈跳变）

**结论**：确诊RL模型问题
**下一步**：执行方案C（添加转动惩罚，重新训练）

---

#### ❌ **情况B：仍然原地转圈**（物理层问题）

**观察到的现象**：
- 机器人仍然旋转（角速度持续 > 0.5 rad/s）
- 线速度接近0
- RViz中没有路径规划（或路径混乱）
- 机器人无法前进

**Gmapping日志**：
- ❌ 仍然出现 "Scan Matching Failed"
- ❌ Likelihood值 < 0（负值）
- ❌ Laser Pose坐标剧烈跳变

**结论**：物理层问题
**可能原因**：
1. 万向轮摩擦力参数（mu1, mu2）设置不当
2. 机器人转动惯量配置错误
3. 轮子与地面接触不稳定

**下一步**：
- 检查 `dashgo_d1_sim.urdf.xacro` 中的万向轮参数
- 调整摩擦力系数（从0.0改为0.1-0.5）
- 检查惯性属性

---

## 📊 验证清单

### 启动阶段
- [ ] Gazebo正常启动
- [ ] RViz正常启动
- [ ] move_base节点正常启动（rqt_graph中可见）
- [ ] Gmapping正常启动
- [ ] 机器人模型spawn成功
- [ ] 激光雷达数据正常（RViz中显示scan）

### TF检查
- [ ] map → odom 变换正常
- [ ] odom → base_link 变换正常
- [ ] base_link → laser_link 变换正常
- [ ] TF_REPEATED_DATA警告减少（或消失）

### 导航测试
- [ ] 能发送2D Nav Goal
- [ ] RViz中显示全局路径
- [ ] RViz中显示局部路径
- [ ] 机器人开始移动
- [ ] 线速度 > 0.1 m/s
- [ ] 角速度平稳（不持续旋转）
- [ ] 能避开障碍物（如果有）
- [ ] 能到达目标点

### SLAM检查
- [ ] Gmapping不报错（或错误减少）
- [ ] 地图正常建立（RViz中白色区域）
- [ ] 机器人定位稳定（不剧烈跳变）
- [ ] Scan Matching成功率提高

---

## 🐛 可能遇到的问题

### 问题1：move_base启动失败

**症状**：
```
[ERROR] [WallTime]: Failed to create the global planner
[ERROR] [WallTime]: Failed to create the local planner
```

**原因**：参数文件路径错误或文件不存在

**解决方案**：
```bash
# 检查参数文件是否存在
ls -lh catkin_ws/src/dashgo_rl/param/

# 如果缺少文件，重新复制
cp dashgo/1/1/nav/param/*.yaml catkin_ws/src/dashgo_rl/param/
```

---

### 问题2：Costmap配置错误

**症状**：
```
[ERROR] [WallTime]: Failed to transform sensor data
[ERROR] [WallTime]: Laser scan observation buffer empty
```

**原因**：激光雷达topic名称不匹配

**解决方案**：
检查 `costmap_common_params.yaml` 中的：
```yaml
observation_sources: laser_scan_sensor
laser_scan_sensor: {sensor_frame: laser_link, ...}
```

确认 `sensor_frame: laser_link` 与URDF中的frame名称一致。

---

### 问题3：DWA规划器失败

**症状**：
```
[WARN] [WallTime]: DWA planner failed to produce path
[WARN] [WallTime]: No valid trajectory found
```

**原因**：
- DWA参数过于保守
- 机器人速度限制太小
- 障碍物太密集

**解决方案**：
调整 `base_local_planner_dwa_params.yaml`：
```yaml
max_vel_x: 0.3        # 确保不是0
min_vel_trans: 0.05   # 降低最小速度
sim_time: 1.7         # 增加前向模拟时间
```

---

### 问题4：TF_REPEATED_DATA警告仍然很多

**症状**：
```
[WARN] [WallTime]: TF_REPEATED_DATA ignoring data with redundant timestamp
for frame left_wheel_link
for frame right_wheel_link
```

**原因**：robot_state_publisher 和 Gazebo 都在发布轮子TF

**影响**：小（TF会取最新时间戳）

**解决方案**（可选）：
修改 `dashgo_d1_sim.urdf.xacro`，禁止Gazebo发布轮子TF：
```xml
<gazebo reference="left_wheel_link">
  <disableFilteredLogging>true</disableFilteredLogging>
</gazebo>
```

---

## 📝 验证记录（待用户填写）

### 测试1：简单场景（无障碍物）

**启动时间**：YYYY-MM-DD HH:MM:SS

**观察到的现象**：
- 机器人行为：____________________
- 线速度：____________________
- 角速度：____________________
- Gmapping日志：____________________

**判断结果**：
- [ ] ✅ 传统导航正常
- [ ] ❌ 仍然转圈

**结论**：____________________

---

### 测试2：障碍物场景（如果测试1成功）

**启动时间**：YYYY-MM-DD HH:MM:SS

**观察到的现象**：
- 机器人避障行为：____________________
- 路径规划质量：____________________
- 成功率：____________________

**判断结果**：
- [ ] ✅ 能避开障碍物
- [ ] ❌ 避障失败

**结论**：____________________

---

## 🚀 下一步行动（根据验证结果）

### 情况A：传统导航正常（确诊RL问题）

**执行方案C**（添加转动惩罚）：

#### 步骤1：修改 `dashgo_env_v2.py`

```python
# 在奖励函数中添加转动惩罚
@configclass
class RewardTermCfg:
    shaping_angular_penalty: RewardTermCfg = RewardTermCfg(
        weight=0.5,  # 与参考项目一致
        params={}
    )

def compute_angular_penalty(self, actions: torch.Tensor) -> torch.Tensor:
    """
    计算转动惩罚

    Args:
        actions: 动作 [num_envs, 2]，actions[:,1]是角速度

    Returns:
        penalty: 惩罚值 [num_envs]
    """
    angular_vel = actions[:, 1]
    penalty = torch.abs(angular_vel)
    return penalty
```

#### 步骤2：清理旧训练日志（DR-018规则）
```bash
rm -rf logs/dashgo_v5_auto
```

#### 步骤3：重新训练
```bash
python scripts/train_v2.py --num_envs 80 --max_iterations 500
```

---

### 情况B：仍然转圈（物理层问题）

#### 步骤1：检查万向轮参数

**文件**：`dashgo_d1_sim.urdf.xacro`

```xml
<!-- 当前配置 -->
<gazebo reference="caster_front_link">
  <mu1>0.0</mu1>  <!-- 静摩擦 -->
  <mu2>0.0</mu2>  <!-- 动摩擦 -->
</gazebo>

<!-- 建议调整为 -->
<gazebo reference="caster_front_link">
  <mu1>0.1</mu1>  <!-- 稍微增加摩擦 -->
  <mu2>0.1</mu2>
</gazebo>
```

#### 步骤2：检查惯性属性

```xml
<!-- 检查机器人质量分布 -->
<inertial>
  <mass value="5.0"/>
  <inertia ixx="0.1" ixy="0.0" ixz="0.0"
            iyy="0.1" iyz="0.0"
            izz="0.1"/>
</inertial>
```

---

## 📚 相关文档

### 问题记录
- `issues/2026-01-29_1745_SLAM定位失败导致原地乱转_架构师诊断.md` - 架构师日志诊断
- `issues/2026-01-29_1800_架构师方案A本地化执行方案.md` - 详细执行方案

### 参考项目分析
- `.claude-temp/drl_nav_analysis/final_report.md` - DRL-robot-navigation项目分析
- `.claude-temp/drl_nav_analysis/04_spinning_issue.md` - 原地乱转诊断

### Git提交
- commit dfd7a27: "feat: 方案A实施 - 禁用RL节点，启用move_base验证"
- commit 51dc67c: "fix: 修复Gmapping失明和move_base未启动问题"

---

## 🎯 关键指标对比

### 修改前（RL控制）
- 控制节点：geo_nav_node.py
- 角速度：~11 rad/s（极端）
- 线速度：~0.98 m/s
- Gmapping：Scan Matching Failed
- 定位：坐标剧烈跳变
- 行为：原地乱转

### 修改后（MoveBase控制，待验证）
- 控制节点：move_base (DWA)
- 角速度：待测试
- 线速度：待测试
- Gmapping：待测试
- 定位：待测试
- 行为：待测试

---

**记录版本**: v1.0
**创建时间**: 2026-01-29 18:10:00
**状态**: ⏸️ 待用户验证
**预计验证时间**: 10-15分钟
**风险等级**: 低（仅验证，不破坏现有功能）
**架构师评分**: 95/100 (接近完美)
