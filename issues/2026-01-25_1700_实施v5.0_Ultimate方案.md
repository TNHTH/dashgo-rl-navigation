# 实施v5.0 Ultimate方案 - 自动课程学习 + 混合奖励

> **实施时间**: 2026-01-25 17:00:00
> **严重程度**: 🟢 功能改进
> **状态**: ✅ 已实施
> **相关文件**: train_cfg_v2.yaml, dashgo_env_v2.py

---

## 问题描述

**从v3_robust_nav升级到v5.0 Ultimate**：

v3_robust_nav的问题：
- ⚠️ action_smoothness几乎为0（Noise风险）
- ⚠️ shaping_distance 0.5过低（收敛慢）
- ⚠️ 无课程学习（远距离目标可能学不会）
- ⚠️ reach_goal奖励1000.0不够突出

**解决方案**：采纳架构师v4.0 Auto-Curriculum建议，实施v5.0 Ultimate方案

---

## 实施内容

### 1. 训练配置修改（train_cfg_v2.yaml）

```yaml
# 修改前（v3_robust_nav）
max_iterations: 4000
run_name: "v3_robust_nav"
save_interval: 50

# 修改后（v5.0 Ultimate）
max_iterations: 5000        # ✅ 给足时间让课程从 3m 走到 8m
run_name: "v5_ultimate_auto"
save_interval: 100          # ✅ 每100轮保存，更安全
```

### 2. 核心功能添加（dashgo_env_v2.py）

#### 2.1 自动课程学习函数

**新增**：`curriculum_expand_target_range()`

```python
def curriculum_expand_target_range(env, env_ids, command_name, start_step, end_step, min_limit, max_limit):
    """
    [v5.0 核心] 自动化课程学习
    根据当前训练总步数，线性扩展目标生成的距离范围 (3m -> 8m)
    """
    current_step = env.common_step_counter

    # 计算进度 alpha (0.0 ~ 1.0)
    if current_step < start_step:
        alpha = 0.0
    elif current_step > end_step:
        alpha = 1.0
    else:
        alpha = (current_step - start_step) / (end_step - start_step)

    # 计算当前难度
    current_limit = min_limit + (max_limit - min_limit) * alpha

    # 动态修改命令生成器的参数
    cmd_term = env.command_manager.get_term(command_name)
    if hasattr(cmd_term.cfg, "ranges") and hasattr(cmd_term.cfg.ranges, "pos_x"):
        cmd_term.cfg.ranges.pos_x = (-current_limit, current_limit)
        cmd_term.cfg.ranges.pos_y = (-current_limit, current_limit)
```

**原理**：
- 使用物理步数（common_step_counter）而非iteration数作为时间基准
- 线性插值保证平滑过渡
- 动态修改命令生成器的配置范围实现难度爬坡

#### 2.2 辅助奖励函数优化

**新增**：简化版`reward_target_speed()`和`reward_facing_target()`

```python
def reward_target_speed(env, asset_cfg):
    """[v5.0] 速度对齐奖励：直接鼓励向前移动"""
    vel = env.scene[asset_cfg.name].data.root_lin_vel_b[:, 0]
    return torch.clamp(vel, min=0.0)

def reward_facing_target(env, command_name, asset_cfg):
    """[v5.0] 对准奖励：鼓励车头朝向目标"""
    target_pos = env.command_manager.get_command(command_name)[:, :2]
    robot_pos = env.scene[asset_cfg.name].data.root_pos_w[:, :2]
    robot_yaw = env.scene[asset_cfg.name].data.heading_w
    target_vec = target_pos - robot_pos
    target_yaw = torch.atan2(target_vec[:, 1], target_vec[:, 0])
    angle_error = torch.abs(mdp.math.wrap_to_pi(target_yaw - robot_yaw))
    return 1.0 / (1.0 + angle_error)
```

### 3. 奖励函数重构（RewardsCfg）

**修改前（v3_robust_nav）**：
```python
reach_goal = 1000.0
shaping_distance = 0.5 (势能差)
action_smoothness = 0.0001 (几乎为0)
collision threshold = 1.0
```

**修改后（v5.0 Ultimate）**：
```python
reach_goal = 2000.0  # ✅ 绝对主导值（提升2倍）
shaping_distance = 0.75 (使用mdp.rewards.position_command_error_tanh)  # ✅ 黄金平衡点
action_smoothness = -0.01  # ✅ 提升100倍
collision threshold = 10.0  # ✅ 放宽到10.0
```

**完整奖励架构**：
```python
@configclass
class DashgoRewardsCfg:
    # [主导] 终点大奖：2000分
    reach_goal = RewardTermCfg(
        func=reward_near_goal,
        weight=2000.0,
        params={"threshold": 0.5}
    )

    # [引导] 黄金平衡点 0.75 + tanh
    shaping_distance = RewardTermCfg(
        func=mdp.rewards.position_command_error_tanh,
        weight=0.75,
        params={"std": 2.0, "command_name": "target_pose"}
    )

    # [辅助] Dense奖励组（保留v3优势）
    target_speed = RewardTermCfg(func=reward_target_speed, weight=1.0)
    facing_goal = RewardTermCfg(func=reward_facing_target, weight=0.1)
    velodyne_style_reward = RewardTermCfg(func=reward_navigation_sota, weight=1.0)

    # [约束] 动作平滑：-0.01
    action_smoothness = RewardTermCfg(
        func=reward_action_smoothness,
        weight=-0.01  # 从0.0001提升100倍
    )

    # [约束] 碰撞惩罚：-50.0 + 10.0阈值
    collision = RewardTermCfg(
        func=penalty_collision_force,
        weight=-50.0,
        params={"threshold": 10.0}
    )
```

### 4. 命令配置修改（CommandsCfg）

**修改前（v3_robust_nav）**：
```python
ranges: mdp.UniformPoseCommandCfg.Ranges(
    pos_x=(0.5, 1.5), pos_y=(-1.0, 1.0),  # 1.5m范围
)
```

**修改后（v5.0 Ultimate）**：
```python
ranges: mdp.UniformPoseCommandCfg.Ranges(
    pos_x=(-3.0, 3.0), pos_y=(-3.0, 3.0),  # ✅ 3m x 3m正方形（新手区）
)
```

### 5. 课程配置新增（CurriculumCfg）

**新增**：`DashgoCurriculumCfg`

```python
@configclass
class DashgoCurriculumCfg:
    """[v5.0 核心] 自动化课程学习配置"""
    target_expansion = CurriculumTermCfg(
        func=curriculum_expand_target_range,
        params={
            "command_name": "target_pose",
            "min_limit": 3.0,  # 初始难度：3米（新手区）
            "max_limit": 8.0,  # 最终难度：8米（专家区）
            "start_step": 0,
            "end_step": 300_000_000,  # 300M物理步完成爬坡
        }
    )
```

### 6. 环境配置更新（DashgoNavEnvV2Cfg）

**新增**：
```python
@configclass
class DashgoNavEnvV2Cfg(ManagerBasedRLEnvCfg):
    # ... 其他配置 ...
    curriculum = DashgoCurriculumCfg()  # ✅ 启用自动课程学习
```

---

## 关键改进点

### 1. 自动课程学习（核心创新）

**优势**：
- ✅ 零干预（Fire & Forget）
- ✅ 自动扩展难度（3m → 8m）
- ✅ 线性插值平滑过渡

**实现方式**：
- 通过`CurriculumTermCfg`注册课程函数
- 在环境重置时自动调用
- 动态修改命令生成器的`ranges`参数

### 2. 混合奖励架构

**设计理念**：
- **Sparse主导**（2000.0）：确保"到达终点"是全局最优解
- **Dense辅助**（1.0+0.1+1.0）：解决初期迷茫，提高学习效率
- **黄金平衡**（0.75+tanh）：提供方向感，tanh限制单步收益防止刷分
- **强约束**（-0.01, -50.0）：抑制抖动，强化避障

### 3. 动作平滑强化

**修改前**：`action_smoothness = 0.0001`（几乎为0）

**修改后**：`action_smoothness = -0.01`（提升100倍）

**预期效果**：治愈Policy Noise 17.30问题

---

## 预期训练曲线

### Phase 1 (0-1000 iter): 新手期
- Policy Noise: 1.0 → 0.8
- reach_goal: 0% → 20%（3m近距离容易达成）
- target range: 3m × 3m

### Phase 2 (1000-3000 iter): 成长期
- Policy Noise: 0.8 → 0.5
- reach_goal: 20% → 40%
- target range: 3m → 8m（自动扩展）

### Phase 3 (3000-5000 iter): 专家期
- Policy Noise: 0.5 → 0.3
- reach_goal: 40% → 60%+
- target range: 8m × 8m（稳定）

---

## 验证方法

### 成功标志

**策略稳定性**：
- ✅ Policy Noise Std < 1.0（全程稳定）
- ✅ 无"爆炸式"上升

**任务完成率**：
- ✅ reach_goal: 1000轮时 > 20%
- ✅ reach_goal: 3000轮时 > 40%
- ✅ reach_goal: 5000轮时 > 60%

**行为观察**：
- ✅ 机器人平滑移动，无高频抖动
- ✅ 能完成3m → 8m的渐进式导航
- ✅ 最终能稳定完成8m长距离导航

### 失败标志

**策略再次爆炸**：
- ❌ Policy Noise > 5.0
- ❌ Noise上升速度 > 0.1/iteration

**课程学习失败**：
- ❌ reach_goal始终 < 10%
- ❌ 无法从3m过渡到8m

---

## 技术合规性检查

**Isaac Lab API**：
- ✅ `CurriculumTermCfg`：官方推荐课程学习方式
- ✅ `common_step_counter`：物理步数基准
- ✅ `mdp.rewards.position_command_error_tanh`：官方tanh奖励函数

**代码质量**：
- ✅ 遵循DRY原则（复用核心工具函数）
- ✅ 添加详细文档字符串
- ✅ 保持与现有代码风格一致

---

## 相关提交

- **本次实施**: v5.0 Ultimate方案（自动课程学习 + 混合奖励）
- **前序版本**: v3_robust_nav（稳健版配置）
- **架构师建议**: v4.0 Auto-Curriculum

---

## 后续优化方向

### 如果v5训练效果理想
1. 考虑进一步优化课程学习曲线（非线性插值）
2. 调整 Dense 奖励权重（找到最优平衡）
3. 实验不同的reach_goal奖励值（2000.0 vs 1000.0 vs 500.0）

### 如果v5训练不理想
**Option A**: 降低reach_goal奖励
```python
reach_goal = 1000.0  # 从2000降到1000
```

**Option B**: 减缓课程学习速度
```python
end_step = 600_000_000  # 从300M步延长到600M步
```

**Option C**: 回退到v3_robust_nav
```bash
git checkout <v3_commit_hash>
```

---

**创建时间**: 2026-01-25 17:00:00
**维护者**: TNHTH
**架构师认证**: ✅ 基于架构师v4.0 Auto-Curriculum建议
**状态**: ✅ 已实施（等待训练验证）
**下一步**: 启动训练，监控Policy Noise和reach_goal曲线
